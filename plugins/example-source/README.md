# example-source

Optional sample extension that writes a fake summary for verifying the local extension host.

## Enable

```bash
cp config/plugins.example.json config/plugins.local.json
# set example-source.enabled = true
```

Restart the dashboard; a sample tab appears only when enabled.
